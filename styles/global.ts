import { FONTS, SIZES } from "@/constants/theme";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  headerText2: { fontFamily: FONTS.RBold, fontSize: SIZES.large },
});

export const global = {
  textColor: "text-slate=700",
};
